@model IEnumerable<AlphaERP.Models.Acc>

    <div class="form-group">
        <div class="col-md-12">
            <table class="table table-hover" id="StageAccExpLink_AccTable">
                <thead>
                    <tr>
                        <th style="width: 20%;">@Html.Recource(255)</th>
                        <th style="width: 55%;">@Html.Recource(256)</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody id="StageAccExpLink_AccTbody">
                    @foreach (var item in Model)
                    {
                        <tr onclick="Select_finItemsAcc(this)" deptid="@item.deptid" Acc_NUM="@item.AccId" AccDesc="@Html.Translate(item.AccDescAr,item.AccDescEn)">
                            <td style="width: 20%;" id="BOM_Acc_NUM">@item.AccId</td>
                            <td style="width: 55%;" id="BOM_AccDesc">@Html.Translate(item.AccDescAr, item.AccDescEn).ToString()</td>
                            <td></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
<script>
    $('#StageAccExpLink_AccTable').DataTable({
        "scrollY": 200,
        "paging": true,
        "pageLength": 25
    });

  
</script>